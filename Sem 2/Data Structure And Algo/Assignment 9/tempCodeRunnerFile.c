numberofzeros = countzero(arr, size);
            // if ((1 - (numberofzeros / size)) >= 0.8)
            // {
            //     size = size * 2;
            //     arr = (int *)realloc(arr, size * sizeof(int));
            // }